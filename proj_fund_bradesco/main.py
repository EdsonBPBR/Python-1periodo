# import sqlite3
# >>> con = sqlite3.connect('proj_fund_bradesco/banco_bradesco.db')

# >>> con = sqlite3.connect('banco_bradesco.db')   
                
# >>> cur = con.cursor()
# >>> cur.execute('CREATE table contato(nome, endereco, telefone)')
# >>> res = cur.execute('SELECT name FROM sqlite_master')
# >>> res.fetchone()
# ('contato',)